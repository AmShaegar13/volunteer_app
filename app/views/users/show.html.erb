<table border="1">
  <tr>
    <td>Summoner:</td><td><%= @user.name %></td>
  <tr>
  </tr>
    <td>Total bans:</td><td><%= @user.bans.count %></td>
  <tr>
<% if @user.banned? -%>
  </tr>
    <td>Banned until:</td><td><%= @user.bans.last.ends %></td>
  </tr>
<% end -%>
</table>
<br/>
<%= render partial: 'bans/list' %>